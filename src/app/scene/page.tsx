"use client"
import ThreeScene from '../ThreeScene';
export default function Scene() {

    return (
        <div className="w-full h-full flex items-center justify-center">
            <ThreeScene x={8.4} y={4.1} z={-9.2} />
        </div>

    );

}